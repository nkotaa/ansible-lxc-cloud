lxc_user: "{{ ansible_user }}"

idmap_start: 100000
max_container_count: 16

nocloud_seedfrom: "file:///etc/cloud/cloud.cfg.d/nocloud-seed/"

default_release:
  alpine: "3.22"
  archlinux: "current"
  debian: "bookworm"
  fedora: "42"
  ubuntu: "noble"
